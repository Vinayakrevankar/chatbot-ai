# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Knowledge base — mirrored from the private S3 bucket with `cx sync`.
# Not committed: one article is a league leaderboard listing ~1000 real player
# display names and their payout amounts.
data/

# Derived from data/ and regenerated by `cx ingest`. chunks.json embeds the
# full article text, so committing it would leak the same content.
index/

# Support transcripts captured by the assistant. Player conversations.
tickets/

# macOS
.DS_Store
